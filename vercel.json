{
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ],
  "buildCommand": "chmod +x build-fix.sh && ./build-fix.sh",
  "installCommand": "npm install --force",
  "framework": "nextjs",
  "env": {
    "NODE_PATH": "src"
  }
} 